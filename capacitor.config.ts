import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'swipe.n.decide',
  appName: 'swipe-n-decide',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
